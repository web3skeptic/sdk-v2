{
  "name": "@circles-sdk/runner",
  "version": "0.1.0",
  "description": "Contract runner implementations for Circles SDK",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node --format esm --splitting --external viem --external @circles-sdk/types && tsc --emitDeclarationOnly",
    "dev": "tsc --build --watch",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "circles",
    "runner",
    "ethereum",
    "viem"
  ],
  "license": "MIT",
  "dependencies": {
    "@circles-sdk/types": "workspace:*",
    "viem": "^2.38.0"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
