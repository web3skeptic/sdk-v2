{
  "name": "@circles-sdk/sdk",
  "version": "0.1.0",
  "description": "Simplified Circles SDK for non-crypto users with low entrance barrier",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --format esm --splitting && tsc --emitDeclarationOnly",
    "dev": "tsc --build --watch",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "circles",
    "sdk",
    "simplified",
    "web3",
    "ubi"
  ],
  "license": "MIT",
  "dependencies": {
    "@circles-sdk/types": "workspace:*",
    "@circles-sdk/events": "workspace:*",
    "@circles-sdk/core": "workspace:*",
    "@circles-sdk/rpc": "workspace:*",
    "@circles-sdk/profiles": "workspace:*",
    "@circles-sdk/utils": "workspace:*",
    "@circles-sdk/runner": "workspace:*",
    "@circles-sdk/pathfinder": "workspace:*",
    "@circles-sdk/transfers": "workspace:*",
    "viem": "^2.38.0"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
